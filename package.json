{
  "name": "sarka-jirka.cz",
  "private": true,
  "scripts": {
    "dev": "npx parcel src/index.html",
    "build": "rm -rf public/* && npx parcel build src/index.html"
  },
  "targets": {
    "production": {
      "distDir": "./public",
      "context": "browser"
    }
  },
  "devDependencies": {
    "@parcel/transformer-stylus": "^2.3.2",
    "parcel": "^2.3.2"
  }
}
